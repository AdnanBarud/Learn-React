import React from 'react'

const Navbar = (props) => {
  return (
    <div>
        <h1>Navbar Component</h1>
        <p>count value from app.jsx is {props.count}</p>
    </div>
  )
}

export default Navbar